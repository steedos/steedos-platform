{
  "name": "steedos",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "npx lerna bootstrap",
    "publish": "npx lerna publish --registry https://registry.npmjs.org"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
