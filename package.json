{
  "name": "steedos",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "publish": "npx lerna publish --registry https://registry.npmjs.org",
    "tsc": "cd packages && cd objectql && tsc && tsc && cd ../auth-utils && tsc && cd ../auth && tsc && cd ../core ",
    "prepare": "lerna run prepare"
  },
  "devDependencies": {
    "lerna": "^3.14.0"
  }
}
