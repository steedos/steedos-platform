 ### 导入操作说明
  - 导入时跟工作区管理员可进行的操作
  - 导入只能在“待执行”视图下进行，选择某一个记录，点击“执行导入”
  - 新增导入对象，给出要导入的数据及要导入数据的表。导入数据的每条记录占一行，每行数据的每一列之间用半角逗号隔开。
  - 导入之后，在“已完成”视图下可查看导入详情。
 ### 导入规则说明
  - text、textarea:文本，直接匹配，直接将表格数据toString()
  - date: 日期，使用new Date()方法，将数据转为日期类型，导入数据转换为时间格式，取“年-月-日”即可，如果格式错误，则暂存错误记录和错误字段。格式如2018-2-2,2018-02-02,2018/02/02,2018/2/2可通过，20180202不能通过。
  - datetime:日期时间，使用new Date()方法，将数据转为日期时间类型。支持格式如：2018-02-02 17:32:22
  - number:数值，将导入数据转化为number类型，导入数值。如12,"12"均可，ming则无法通过。
  - sleect:下拉框，取该字段的allowedValues值匹配，存在则导入，不存在则暂存错误记录和错误字段
  - boolean：判断导入数据，如果为“ 是 / 1 / true / yes / ... ”则表示为true。如果为“ 否 / 0 / false / no / ... ”则表示为false，不填则表示为null。
  - lookup 、master_detail: 找出该字段对应reference_to的值。根据相关表配置的匹配字段，然后取reference_to表的is_name字段值进行查找，找不到则值为空，找到则取第一条的_id保存。
  - 错误信息：错误信息可在导入完成后在“已完成”视图下查看，每条数据对应一个错误信息。错误信息 = 数据内容 + 出错原因。