export enum SteedosFieldDBType {
    varchar = 'varchar',
    text = 'text',
    number = 'number',
    boolean = 'boolean',
    date = 'date',
    dateTime = 'dateTime',
    json = 'json',
    array = 'array',
    // oneToOne,
    // oneToMany,
    // manyToOne,
    // manyToMany
}