name: create
amis:
  disabledOn: '${is_system == true}'
defaultValue: true
hidden: false
label: 弹出选择时允许新建
sort_no: 110
type: boolean
visible_on: '{{[''lookup''].indexOf(formData.type) > -1 ? true: false}}'
