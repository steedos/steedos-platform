name: summary_filters
label: Filter Criteria
type: table
inlineHelpText: >-
  Only records meeting certain criteria should be included in the calculation,
  all records should be included in the calculation while the criteria is empty.
is_wide: true
depend_on:
  - summary_object
visible_on: '{{formData.type === ''summary'' && global.mode !=''read'' ? true: false}}'
sort_no: 470
amis:
  disabledOn: '${is_system == true}'
  inlineEditMode: true
  showIndex: true
