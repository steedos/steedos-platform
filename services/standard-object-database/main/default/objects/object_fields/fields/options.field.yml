name: options
type: table
label: Options
is_wide: true
inlineHelpText: >-
  The label and value of each option cannot be empty. Set the background color
  to FFFFFF in hexadecimal format.
visible_on: '{{formData.type === ''select'' ? true: false}}'
required: '{{formData.type === ''select'' ? true: false}}'
sort_no: 330
amis:
  showIndex: true
  disabledOn: '${is_system == true}'
  enableDialog: false
  columns:
    - label
    - value
    - name: color
      width: 170
    - name: description
      width: 300
