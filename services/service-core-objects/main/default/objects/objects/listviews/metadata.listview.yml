name: metadata
columns:
  - label
  - field: name
    amis:
      copyable: true
  - in_development
  - is_system
  - is_customize
extra_columns:
  - datasource
  - fields
  - is_deleted
filter_scope: space
label: Metadata
shared: true
shared_to: space
sort: [["created", "desc"], ["name", "asc"]]
filters:
  - - isMetadata
    - =
    - true
