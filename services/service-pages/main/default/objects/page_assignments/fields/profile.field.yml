name: profile
type: lookup
label: 简档
reference_to: permission_set
filters:
  - ["type","=","profile"]
visible_on: "{{formData.app ? true: false}}"
sort_no: 125
required: "{{formData.app ? true: false}}"